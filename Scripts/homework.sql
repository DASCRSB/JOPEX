/* The aggregate function COUNT is used to find the number of customers wave has*/
SELECT * FROM COUNT(u_id);

/* The aggregate function COUNT is used to find the number of customers wave has*/
SELECT * FROM COUNT(dest_wallet_id);